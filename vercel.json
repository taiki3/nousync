{
  "version": 2,
  "installCommand": "pnpm -w install",
  "buildCommand": "pnpm -w build",
  "builds": [
    { "src": "apps/api/api/**/*.ts", "use": "@vercel/node" }
  ],
  "outputDirectory": "apps/web/dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "apps/api/api/$1.ts" }
  ]
}
